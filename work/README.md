# 作業用フォルダ

Skill が実行中に使用する中間物を格納します。利用者が事前にファイルを置く必要はありません。

実行時に次のサブフォルダを作成します。

- `converted-office/`: 旧 Office 形式から変換した作業用ファイル
- `images/`: Office/PDF から抽出または描画した画像
- `markdown/`: レビュー用に変換した Markdown

中間物は通常の Git 管理対象になりません。
